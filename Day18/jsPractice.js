// 7.4, 2.6, 234.2, 5.9, 63.5, -57.7, -200.5, 87.3

// 1 - Round the above numbers to the nearest integer and log the result to the console
const myNumbers = [7.4, 2.6, 234.2, 5.9, 63.5, -57.7, -200.5, 87.3];
console.log(myNumbers);
// 2 - Round the above numbers DOWN to the nearest integer and log the result to the console

// 3 - Round the above numbers UP to the nearest integer and log the result to the console

// You may wish to look at the MDN docs for Math.floor, Math.ceil & Math.round