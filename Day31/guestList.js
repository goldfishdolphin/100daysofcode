function makeGuestList(person) {
    // Your code goes here...
    person={name:' '}
    for (let key in person){
     let newName=  [person.name[key].split(',')]
     console.log(person.key)
    }
}    
